{
  "name": "asyncapi-portal",
  "version": "0.0.1",
  "description": "Simple portal to display multiple (connected) asyncapi documentations",
  "main": "src/index.js",
  "scripts": {
    "build": "npm run build:browser && npm run build:d3:visualizer",
    "build:browser": "esbuild src/main.browser.ts --bundle --outfile=output/bundle.browser.js",
    "build:d3:visualizer": "esbuild src/visualizer/d3/htmlRenderer.template.node.js --bundle --outfile=output/bundle.visualizer.d3.template.js",
    "start": "ts-node --project tsconfig.json --files src/index.node.ts",
    "test": "jest --passWithNoTests",
    "lint": "prettier -c ./src",
    "lint:fix": "prettier -w ./src"
  },
  "author": "Timon Back",
  "license": "MIT",
  "devDependencies": {
    "jest": "^29.3.1",
    "prettier": "^2.8.1",
    "ts-node": "^10.9.1"
  },
  "dependencies": {
    "@asyncapi/parser": "^1.17.2",
    "esbuild": "^0.16.12",
    "typescript": "^4.9.4"
  },
  "browser": {
    "@asyncapi/parser": "./node_modules/@asyncapi/parser/dist/bundle.js"
  }
}
